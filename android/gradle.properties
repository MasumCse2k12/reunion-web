org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# Machine-specific JDK path belongs in local.properties or JAVA_HOME, not here —
# a hardcoded path breaks the build for everyone on a different machine/OS.
android.useAndroidX=true
android.enableJetifier=true
