// Top-level build file.
plugins {
    // 8.11+ is the first AGP line that can compile against SDK 36, which Play
    // now requires. Needs Gradle 8.13+ (see gradle/wrapper/gradle-wrapper.properties).
    id 'com.android.application' version '8.13.2' apply false
}
