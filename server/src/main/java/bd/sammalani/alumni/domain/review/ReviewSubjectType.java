package bd.sammalani.alumni.domain.review;

public enum ReviewSubjectType {
    /** The submission for an event. */
    REGISTRATION,
    /** Whether this person is genuinely of the batch they claim. */
    PERSON_VERIFICATION,
    /** Whether the money the member reported actually arrived. */
    PAYMENT
}
